import 'package:flutter/material.dart';

class Echartapp extends StatelessWidget{
  const Echartapp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Center(child: Text('Echart Representation')),

      )
    );
  }}